/**
 * ENTRY POINT — pure HTML + CSS + JavaScript.
 *
 * There is no framework anywhere in this project. This file simply boots the
 * vanilla ES-module application that wires up the 3D engine, the scroll
 * choreography and every interaction. All markup lives in index.html; all
 * behaviour lives in src/js/; all styling lives in src/styles/ (imported by
 * the bootstrap module).
 */

import "./js/bootstrap.js";
